apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: habits-data
  name: habits-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
