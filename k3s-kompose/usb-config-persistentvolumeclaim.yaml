apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: usb-config
  name: usb-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
