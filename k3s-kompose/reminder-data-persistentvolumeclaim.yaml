apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: reminder-data
  name: reminder-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
