apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: library-data
  name: library-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
