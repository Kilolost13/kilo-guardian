apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: financial-data
  name: financial-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
