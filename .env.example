# ====================================
# KILO GUARDIAN - Environment Variables
# ====================================

# Air-gapped deployment: network access disabled by default
ALLOW_NETWORK=false

# Environment placeholders for ai_brain STT/TTS providers
# For air-gapped: use 'none' (default) or local providers: 'vosk' (STT), 'pyttsx3' (TTS)
# For cloud-enabled: use 'openai' (STT), 'gtts' (TTS) - requires ALLOW_NETWORK=true
STT_PROVIDER=none
TTS_PROVIDER=none

# If using an external provider, set its API key here (requires ALLOW_NETWORK=true)
PROVIDER_API_KEY=

# ====================================
# SECURITY SETTINGS
# ====================================

# Library of Truth admin key - MUST be set before deployment
# Generate a secure random 32+ character string for production
# Example: openssl rand -hex 32
# DO NOT use any default value in production
LIBRARY_ADMIN_KEY=GENERATE_SECURE_RANDOM_32_CHAR_STRING_HERE

# CORS allowed origins (comma-separated)
# Default: localhost only. For production, specify exact origins
# Example: http://yourdomain.com,https://yourdomain.com
CORS_ORIGINS=http://localhost:3000,http://localhost:30000

# Gateway database URL
# Default: /data/gateway.db (persistent)
# Do NOT use /tmp in production (data will be lost on restart)
GATEWAY_DB_URL=sqlite:////data/gateway.db

# ====================================
# SERVICE CONFIGURATION
# ====================================

# Gateway URL used by integration tests (leave as-is for local compose)
GATEWAY_URL=http://127.0.0.1:8001
